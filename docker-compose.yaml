version: "3.9"

services:
  matrix-mqtt-bridge:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: matrix-mqtt-bridge
    restart: always
    volumes:
      - ./config.ini:/config.ini
