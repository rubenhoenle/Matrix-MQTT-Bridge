name: CI/CD

on:
  push:
    branches: [ "main", "feat/ci-pipeline" ]
    tags: ['v*']
  pull_request:
    branches: [ "main" ]

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    outputs:
      imageid: ${{ steps.build-docker-image.outputs.imageid }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Setup Docker buildx
        uses: docker/setup-buildx-action@v3

      - name: Log into registry ${{ env.REGISTRY }}
        if: ${{ github.event_name != 'pull_request' }}
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

      - name: Build docker image
        id: build-docker-image
        uses: docker/build-push-action@v5
        with:
          context: .
          #platforms: linux/amd64,linux/arm64,linux/arm/v7
          #push: ${{ github.event_name != 'pull_request' }}
          push: false
          #tags: ${{ steps.meta.outputs.tags }}
          tags: ${{ env.REGISTRY }}/rubenhoenle/matrix-mqtt-bridge:ci-cd
          labels: ${{ steps.meta.outputs.labels }}
          outputs: type=docker,dest=/tmp/image.tar
      
      - name: Upload docker image as artifact
        uses: actions/upload-artifact@v4
        with:
          name: dockerimage
          path: /tmp/image.tar

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: [ build ]

    env:
      # MQTT, unencrypted, authenticated
      MQTT_HOST: test.mosquitto.org
      MQTT_PORT: 1884
      MQTT_USER: rw
      MQTT_PASS: readwrite
      MQTT_TLS: false
      MQTT_TOPIC_BRIDGE_INPUT: mqttbridge/sub
      MQTT_TOPIC_BRIDGE_OUTPUT: mqttbridge/pub 

    steps:
      - name: Download dockerimage from build artifacts
        uses: actions/download-artifact@v4
        with:
          name: dockerimage
          path: /tmp
      
      - name: Load dockerimage
        run: |
          docker load --input /tmp/image.tar
          docker image ls -a 

      - name: Install matrix cli
        run: pip3 install matrix-commander

      - name: Login with matrix cli
        run: | 
          matrix-commander --login password \
            --user-login "${{ secrets.MATRIX_USERNAME }}" \
            --password "${{ secrets.MATRIX_PASSWORD }}" \
            --homeserver "${{ secrets.MATRIX_HOMESERVER }}" \
            --device "Matrix-MQTT-Bridge-CI-CD" \
            --room-default "${{ secrets.MATRIX_ROOMID }}"

      - name: Create config.ini file
        run: |
          echo "[MATRIX]"                                             > ${{ env.FILENAME }}
          echo "homeserver = ${{ secrets.MATRIX_HOMESERVER }}"        >> ${{ env.FILENAME }}
          echo "user = ${{ secrets.MATRIX_USERNAME }}"                >> ${{ env.FILENAME }}
          echo "password = ${{ secrets.MATRIX_PASSWORD }}"            >> ${{ env.FILENAME }}
          echo "room_id = ${{ secrets.MATRIX_ROOMID }}"               >> ${{ env.FILENAME }}
          echo ""                                                     >> ${{ env.FILENAME }}
          echo "[MQTT]"                                               >> ${{ env.FILENAME }}
          echo "host = ${{ env.MQTT_HOST }}"                          >> ${{ env.FILENAME }}
          echo "user = ${{ env.MQTT_USER }}"                          >> ${{ env.FILENAME }}
          echo "password = ${{ env.MQTT_PASS }}"                      >> ${{ env.FILENAME }}
          echo "port = ${{ env.MQTT_PORT }}"                          >> ${{ env.FILENAME }}
          echo "tls = ${{ env.MQTT_TLS }}"                            >> ${{ env.FILENAME }}
          echo "topic_sub = ${{ env.MQTT_TOPIC_BRIDGE_INPUT }}"       >> ${{ env.FILENAME }}
          echo "topic_pub = ${{ env.MQTT_TOPIC_BRIDGE_OUTPUT}}"       >> ${{ env.FILENAME }}
        env:
          FILENAME: config.ini

      - name: Start Matrix-MQTT-Bridge docker container
        run: docker run -d -v ./config.ini:/config.ini ${{ needs.build.outputs.imageid }}

  push:
    name: Push docker image
    runs-on: ubuntu-latest
    needs: [ test ]
    
    steps:
      - name: Download dockerimage from build artifacts
        uses: actions/download-artifact@v4
        with:
          name: dockerimage
          path: /tmp
      
      - name: Load dockerimage
        run: |
          docker load --input /tmp/image.tar
          docker image ls -a 


